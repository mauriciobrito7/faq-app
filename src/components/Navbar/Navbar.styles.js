import styled from 'styled-components';
import { Heading } from '@chakra-ui/react';

export const Band = styled.div`
  width: 100%;
  height: 15px;
  background: linear-gradient(122deg, #00ffcd, #0073ff);
`;

export const Header = styled(Heading)`
  font-size: '128px';
  font-weight: 'black';
`;
